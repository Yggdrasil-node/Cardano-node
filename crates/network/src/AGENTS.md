# Guidance for concrete network transport, mux, and client-driver implementation work
Focus on implementation details for bearer I/O, mux/demux behavior, protocol driver ergonomics, and wire-level safety properties.

## Scope
- `bearer.rs`, `multiplexer.rs`, `mux.rs`, `peer.rs`, `peer_selection.rs`, typed client/server drivers, and peer governor.
- CBOR message boundary handling, segmentation/reassembly, and protocol-handle composition.

##  Rules *Non-Negotiable*
- Keep wire framing deterministic and byte-accurate.
- Do not leak protocol business logic from `protocols/` state machines into transport primitives.
- Keep reusable topology types, peer candidate resolution, bootstrap-target sequencing, reconnect attempt ordering, and preferred-peer retry state here rather than in `node`, while avoiding full peer-governor state in low-level transport helpers.
- Preserve strict separation between raw transport (`ProtocolHandle`) and higher-level message orchestration (`MessageChannel`, client drivers).
- Any receive-path buffering or boundary detection changes MUST ship with regression tests for partial/incremental payload delivery.
- Public transport and driver APIs MUST include Rustdocs when behavior is non-obvious.
- Stay true to the official type naming and terminology for node concepts, network protocols, and ledger types when possible.
- Always read the folder specific `**/AGENTS.md` files. They MUST stay current and MUST remain operational rather than long-form documentation. If the folder context is outdated, missing, or incorrect, update the relevant `AGENTS.md` file.

## Official Upstream References *Always research references and add or update links as needed*
- [Multiplexer (`network-mux`):](https://github.com/IntersectMBO/ouroboros-network/tree/main/network-mux)
- [Network framework and handshake:](https://github.com/IntersectMBO/ouroboros-network/tree/main/ouroboros-network-framework)
- [Typed mini-protocol implementations:](https://github.com/IntersectMBO/ouroboros-network/tree/main/ouroboros-network-protocols)
- [Outbound governor and peer selection:](https://github.com/IntersectMBO/ouroboros-network/tree/main/ouroboros-network)
- [Cardano diffusion layer:](https://github.com/IntersectMBO/ouroboros-network/tree/main/cardano-diffusion)
- [Shelley networking spec PDF:](https://ouroboros-network.cardano.intersectmbo.org/pdfs/network-spec)
- [Network design document:](https://ouroboros-network.cardano.intersectmbo.org/pdfs/network-design)
- [Network Haddock:](https://ouroboros-network.cardano.intersectmbo.org/)

## Current Phase
- TCP bearer and SDU framing are implemented and tested.
- Mux/demux routing is implemented with per-protocol egress channels, per-protocol ingress/egress byte accounting, configurable ingress queue limits (`DEFAULT_INGRESS_LIMIT` = 2 MB), egress soft buffer limit (`EGRESS_SOFT_LIMIT` = 0x3ffff), and weighted round-robin egress scheduling (`DEFAULT_PROTOCOL_WEIGHT`, configurable via `ProtocolConfig`). `MuxError::IngressQueueOverRun` and `MuxError::EgressBufferOverflow` terminate connections that exceed resource bounds. `start()` remains backward-compatible; `start_configured()` / `start_unix_configured()` accept per-protocol `ProtocolConfig` for custom limits and weights. `WeightHandle` enables runtime-dynamic scheduling weight adjustment: shared atomically between `ProtocolHandle` and the mux writer, changes take effect immediately. `ProtocolHandle::weight_handle()` clones the shared handle before the protocol handle is consumed by a client driver.
- Large-message SDU segmentation/reassembly is implemented via `MAX_SEGMENT_SIZE` + `MessageChannel`.
- Typed ChainSync, BlockFetch, KeepAlive, and TxSubmission client drivers are in place. TxSubmission now uses typed ledger `TxId` values for request/advertise flows, provides typed reply helpers for both `Vec<Tx>` and `Vec<MultiEraSubmittedTx>`, and maintains an outstanding/requestable TxId FIFO so invalid acknowledgements and transaction requests are rejected before replying while preserving raw wire bodies.
- Server-side (responder) drivers are now implemented for all four data mini-protocols: `KeepAliveServer`, `BlockFetchServer`, `ChainSyncServer`, and `TxSubmissionServer`. Each server driver wraps `MessageChannel`, maintains the protocol state machine, and provides typed send/receive methods. The server drivers follow the same pattern as the client drivers but handle server-agency states.
- Topology domain model work is now in this crate: local roots carry `hotValency`, `warmValency`, `diffusionMode`, and trustability, while public roots remain a separate type.
- Root-set provider work has started here as well: topology parsing now feeds a resolved provider snapshot, mutable `RootPeerProviderState`, and a refresh-oriented provider API that enforce local-root versus bootstrap/public-root precedence and disjointness. Provider refreshes can also reconcile the `PeerRegistry` on the same crate-owned path, and a DNS-backed root-peer provider now re-resolves configured local-root, bootstrap, and public-root access points without involving `node`. An optional `DnsRefreshPolicy` gates re-resolution behind a time-based schedule with exponential backoff on stale results (defaults matching upstream `clipTTLBelow` 60 s / `clipTTLAbove` 900 s).
- A minimal peer registry now lives here too: `PeerRegistry` tracks `PeerSource` and `PeerStatus` per peer and can reconcile root-provider snapshots plus ledger, big-ledger, and peer-share source sets while preserving unrelated sources and peer status.
- Ledger peer provider work is now complete: `LedgerPeerProvider` trait, `LedgerPeerSnapshot` normalization (deduplicates and enforces disjoint ledger/big-ledger sets), `LedgerPeerProviderRefresh` (combined/per-kind), `apply_ledger_peer_refresh()` helper, `refresh_ledger_peer_registry()` orchestration, and policy helpers `judge_ledger_peer_usage()` plus `reconcile_ledger_peer_registry_with_policy()` for `useLedgerAfterSlot`, latest-slot, ledger-state judgement, and peer-snapshot freshness gating. Provider refreshes reconcile the `PeerRegistry` on crate-owned paths without node involvement.
- Peer governor module (`governor.rs`) now provides a pure decision engine: `GovernorTargets` (target_known/established/active plus big-ledger variants, upstream `sanePeerSelectionTargets` validation), `LocalRootTargets` (per-group warm/hot valency from `LocalRootConfig`, `trustable` flag), and `GovernorAction` (PromoteToWarm/PromoteToHot/DemoteToWarm/DemoteToCold/ForgetPeer/ShareRequest/RequestPublicRoots/RequestBigLedgerPeers/AdoptInboundPeer). Evaluation functions: `evaluate_cold_to_warm_promotions`, `evaluate_warm_to_hot_promotions`, `evaluate_hot_to_warm_demotions`, `evaluate_warm_to_cold_demotions`, `enforce_local_root_valency`, `evaluate_cold_to_warm_big_ledger_promotions`, `evaluate_warm_to_hot_big_ledger_promotions`, `evaluate_hot_to_warm_big_ledger_demotions`, `evaluate_warm_to_cold_big_ledger_demotions`, `evaluate_forget_cold_peers`, and `governor_tick` compute actions from current `PeerRegistry` state. Regular evaluation functions use `RegularPeerCounts` / `regular_peer_counts()` to exclude big-ledger peers from regular target accounting (disjoint target sets matching upstream). `GovernorState` carries: mutable failure-tracking with time-based exponential backoff and decay (`PeerFailureRecord`, `record_success`/`record_failure`, `is_backing_off(now)`, `filter_backed_off(now)`, `failure_decay`, `decayed_failure_count`, `prune_decayed_failures`), in-flight promotion tracking (`mark_in_flight_warm`/`mark_in_flight_hot`, `clear_in_flight_warm`/`clear_in_flight_hot`), and two-phase churn (`ChurnPhase` state machine: `Idle` → `DecreasedActive` → `DecreasedEstablished` → `Idle`, driven by `advance_churn(now)` with mode-dependent intervals from `ChurnConfig`). `ChurnConfig` now carries `bulk_churn_interval` (900 s, upstream `defaultBulkChurnInterval`), `deadline_churn_interval` (3300 s, upstream `defaultDeadlineChurnInterval`), and `phase_timeout`; `interval_for_mode(FetchMode)` selects the appropriate interval. `apply_churn_to_targets()` uses regime-aware decrease functions (`churn_decrease_active`, `churn_decrease_established`) that vary decrease aggressiveness by `ChurnRegime`. `churn_decrease()` implements the upstream `decrease v = max 0 $ v - max 1 (v div 5)`. `ChurnMode` (`Normal`/`BulkSync`) derived from `FetchMode` via `churn_mode_from_fetch_mode()`. `ChurnRegime` (`ChurnDefault`/`ChurnPraosSync`/`ChurnBootstrapPraosSync`) derived from `(ChurnMode, UseBootstrapPeers, ConsensusMode)` via `pick_churn_regime()`. `churn_decrease_active()` implements upstream `decreaseActive`: ChurnDefault uses standard 20% decrease; PraosSync/BootstrapPraosSync cap to `min(max(1, localRootHotTarget), base-1)`. `churn_decrease_established()` implements upstream `decreaseEstablished`: Default/PraosSync shrink only the warm portion `decrease(est-active)+active`; BootstrapPraosSync aggressively sets `min(active, est-1)`. `ConsensusMode` (`PraosMode`/`GenesisMode`) mirrors upstream. `GovernorState` now carries `churn_regime`, `fetch_mode`, and `local_root_hot_target` (auto-updated from local root groups at each tick). Bootstrap-sensitive mode (`PeerSelectionMode::Sensitive`/`Normal`): `requires_bootstrap_peers()`, `peer_selection_mode()`, `is_node_able_to_make_progress()` implement upstream `Cardano.Network.PeerSelection.Bootstrap` logic. In sensitive mode `governor_tick` demotes non-trustable peers (via `evaluate_sensitive_hot_demotions`/`evaluate_sensitive_warm_demotions`), filters promotions to trustable-only (`filter_sensitive_promotions`), and suppresses big-ledger promotions. Trustability is determined by `PeerSourceBootstrap` or membership in a `LocalRootTargets` group with `trustable == true`. `has_only_trustable_established_peers()` reports whether all warm/hot peers are trustable. `PeerRegistryEntry` carries upstream `knownPeerTepid` (`tepid: bool`): set on hot→warm demotion, cleared on cold→warm promotion; promotion functions (`evaluate_cold_to_warm_promotions`, `evaluate_warm_to_hot_promotions`) deprioritize tepid peers. `GovernorState` carries `max_connection_retries: Option<u32>` (upstream `reportFailures` `maxFail`); `evaluate_forget_failed_peers` forgets cold ephemeral peers exceeding the retry threshold while protecting local-root, bootstrap, ledger, and big-ledger sources. `evaluate_forget_cold_peers` now enforces the one-sided `target_root` floor by only forgetting public-root peers when regular root count is above `target_root`. In-flight demotion tracking (`in_flight_demote_warm`/`in_flight_demote_hot` — upstream `inProgressDemoteWarm`/`inProgressDemoteHot`) prevents duplicate demotion actions; `filter_backed_off` filters both promotions and demotions. Peer-sharing request budget (`in_progress_peer_share_reqs`/`max_in_progress_peer_share_reqs` — upstream `inProgressPeerShareReqs`/`policyMaxInProgressPeerShareReqs`); `evaluate_known_peer_discovery` mirrors upstream `KnownPeers.belowTarget`: coin-flip inbound-vs-peer-share selection with inbound retry gating (60s) and max 10 inbound peers per adoption round. `evaluate_peer_share_requests` generates `ShareRequest` actions when known < target_known and budget allows (Normal mode only). `NodePeerSharing` enum (`PeerSharingDisabled`/`PeerSharingEnabled`) mirrors upstream `PeerSharing` willingness flag; `from_wire()` maps handshake byte. `AssociationMode` enum (`LocalRootsOnly`/`Unrestricted`) mirrors upstream `AssociationMode`; `compute_association_mode()` implements upstream `readAssociationMode` from `cardano-diffusion`. In `LocalRootsOnly` mode `governor_tick` Normal path suppresses big-ledger promotions and peer sharing requests. `PeerSelectionCounters` (upstream `PeerSelectionView Int`) provides structured governor counters across four categories (regular, big-ledger, local-root, non-root) with in-flight action counts from `GovernorState`; `from_registry()` implements upstream `peerSelectionStateToView`. `OutboundConnectionsState` (`TrustedStateWithExternalPeers`/`UntrustedState`) mirrors upstream trust signal; `compute_outbound_connections_state()` implements the upstream `outboundConnectionsState` function branching on `AssociationMode` and `UseBootstrapPeers`. `FetchMode` (`FetchModeBulkSync`/`FetchModeDeadline`) mirrors upstream block-fetch concurrency mode; `fetch_mode_from_judgement()` derives mode from `LedgerStateJudgement`. `PeerSelectionTimeouts` groups upstream policy time constants from `simplePeerSelectionPolicy` (find_public_root_timeout, peer_share_retry_time, peer_share_batch_wait_time, peer_share_overall_timeout, peer_share_activation_delay, max_connection_retries, clear_fail_count_delay, inbound_peers_retry_delay, max_inbound_peers). `ConnectionManagerCounters` (upstream `ConnectionManagerCounters` from `ConnectionManager.Types`) tracks full_duplex_conns/duplex_conns/unidirectional_conns/inbound_conns/outbound_conns/terminating_conns with `from_registry()` deriving approximate counts from PeerRegistry and field-wise `Add`. Randomized peer selection via `PickPolicy` (upstream `simplePeerSelectionPolicy` PickPolicy callbacks): `Xorshift64` lightweight PRNG (no `rand` crate dependency), `PickPolicy::pick(count, candidates)` for uniform random subset selection, `PickPolicy::pick_scored(count, candidates, metrics)` for score-weighted selection (hot demotion). `PeerMetrics` tracks per-peer `upstreamyness` (header tip timeliness) and `fetchyness` (block delivery timeliness) with `combined_score()` for scoring hot peers during demotion (upstream `hotDemotionPolicy`). All evaluation functions (`evaluate_cold_to_warm_promotions`, `evaluate_warm_to_hot_promotions`, `evaluate_hot_to_warm_demotions`, `evaluate_warm_to_cold_demotions`, `enforce_local_root_valency`, all big-ledger variants, `evaluate_forget_cold_peers`, `evaluate_peer_share_requests`) now take `&mut PickPolicy` for randomized candidate selection; `evaluate_hot_to_warm_demotions` additionally takes `&PeerMetrics` for score-aware demotion. `governor_tick` and `GovernorState::tick()` thread `PickPolicy` and `PeerMetrics` through all sub-calls. 171 governor tests.
- PeerSharing protocol (mini-protocol 10) is now implemented: `PeerSharingState` state machine + `PeerSharingMessage` (MsgShareRequest/MsgSharePeers/MsgDone) + `SharedPeerAddress` (IPv4/IPv6 CBOR codec) in `protocols/peer_sharing.rs`. Client driver `PeerSharingClient` (`peersharing_client.rs`) and server driver `PeerSharingServer` (`peersharing_server.rs`) with serve_loop callback. 8 protocol-level tests.
- Runtime integration status: the node runtime now opens mini-protocol 10 on standard node-to-node sessions, advertises `peer_sharing = 1` during bootstrap proposals, and can reconcile peer-sharing discoveries into `PeerRegistry` as `PeerSourcePeerShare`. `PeerRegistry` now also tracks per-peer `hot_tip_slot` metadata and exposes `preferred_hot_peer()`, `hot_peers_by_reconnect_priority()`, and `hot_tip_slot()` so reconnect paths can prefer and front-load the most advanced known hot peers and trigger proactive session handoff to higher-tip hot peers. `set_hot_tip_slot()` only applies to peers currently marked `PeerHot`, preventing stale tip metadata on warm/cold entries. Remaining work is broader multi-protocol hot scheduling and any additional responder-side runtime usage.
- Inbound governor module (`inbound_governor.rs`): pure step-function decision engine for inbound connection lifecycle, remote peer state tracking, and duplex peer maturation. Mirrors the outbound governor pattern (pure decisions, effectful CM calls deferred to `node/`). `InboundConnectionEntry` per-connection state (conn_id, remote_st, data_flow, responder_counters, idle_since_ms, connected_at_ms). `InboundGovernorAction` (4 variants: PromotedToWarmRemote, DemotedToColdRemote, ReleaseInboundConnection, UnregisterConnection). `InboundGovernorState` carries connections map, mature/fresh duplex peer maps, counters, and configurable protocol_idle_timeout. `step()` processes all 10 `InboundGovernorEvent` variants via dedicated handlers: new_connection (registers RemoteIdleSt, tracks duplex for maturation), mux_finished (unregisters), mini_protocol_terminated (runtime no-op), wait_idle_remote (warm/hot→idle, emits DemotedToColdRemote), awake_remote (idle/cold→warm, emits PromotedToWarmRemote), remote_promoted_to_hot (warm→hot), remote_demoted_to_warm (hot→warm), commit_remote (idle→release, emits ReleaseInboundConnection), matured_duplex_peers (runs mature_peers), inactivity_timeout (mature + expired idle scan). `apply_commit_result()` processes CM DemotedToColdRemoteTr response (CommitTr→unregister, KeepTr→RemoteColdSt). `mature_peers()` promotes fresh duplex peers past 15-min MATURE_PEER_DELAY. `expired_idle_connections()` scans for timed-out idle connections. `update_responder_counters()` derives IG events from counter changes (upstream inboundGovernorMuxTracer logic). `verify_remote_transition()` validates all 12 valid remote state transitions. 43 tests covering all event types, lifecycles, multi-peer scenarios, edge cases.
- Connection manager module (`connection.rs`): upstream `Ouroboros.Network.ConnectionManager.Types` and `State` type system is now implemented. Core enums: `Provenance` (Inbound/Outbound), `DataFlow` (Unidirectional/Duplex), `TimeoutExpired` (Expired/Ticking). `ConnectionType` (derived from state). `AbstractState` (12-variant tracing-level state) with `is_inbound_conn()` and `connection_type()`. Full `ConnectionState` (10-variant runtime state machine) with `abstract_state()` and `conn_id()` projection. `ConnectionId` (local+remote SocketAddr). `ConnStateId` (unique entry id). `connection_state_to_counters()` maps each state to upstream-correct `ConnectionManagerCounters` per `connectionStateToCounters` from `ConnectionManager.Core`. `verify_abstract_transition()` validates the upstream state-machine transition table (outbound path, inbound path, duplex, self-connect/overwrite races, terminal). `AcceptedConnectionsLimit` (hard_limit 512, soft_limit 384, delay 5s) with `accept_delay()` rate-limiting. `MaybeUnknown<S>` (Known/Race/Unknown), `Transition` (from/to), `OperationResult<A>`, `DemotedToColdRemoteTr` (CommitTr/KeepTr). `ConnectionManagerError` (9 variants). Timeout constants: TIME_WAIT_TIMEOUT (60s), PROTOCOL_IDLE_TIMEOUT (5s), RESET_TIMEOUT (5s), SDU_TIMEOUT (30s), SDU_HANDSHAKE_TIMEOUT (10s), LOCAL_PROTOCOL_IDLE_TIMEOUT (2s), LOCAL_TIME_WAIT_TIMEOUT (0s), PROTOCOL_RECV_TIMEOUT (60s, upstream `shortWait` — used by all N2N server drivers to bound per-message recv; TxSubmission blocking requests exempt). Inbound governor types: `RemoteSt` (RemoteWarmSt/RemoteHotSt/RemoteIdleSt/RemoteColdSt) with `is_established()`, `InboundGovernorCounters` (4-field cold/idle/warm/hot with `count_state()`, `total()`, `Add`), `ResponderCounters`, `InboundGovernorEvent` (10 variants). Inbound constants: MATURE_PEER_DELAY (900s), INACTION_TIMEOUT (~31.4s). 49 connection module tests.
- Connection manager decision engine (`connection_manager.rs`): pure state-machine module implementing upstream `Ouroboros.Network.ConnectionManager.Core` operations. `ConnectionManagerState` (connections map keyed by SocketAddr, monotonic ConnStateId counter, AcceptedConnectionsLimit). `ConnectionEntry` (conn_state_id + ConnectionState). `CmAction` enum (StartConnect/TerminateConnection/StartResponderTimeout/PruneConnections) for runtime side effects. 7 core operations: `acquire_outbound_connection` (reserve → connect, reuse idle inbound duplex → OutboundDupSt, promote active inbound duplex → DuplexSt, self-connect/overwrite handling, Terminating/Terminated passthrough), `outbound_handshake_done` (Reserved/Unnegotiated → OutboundUni/OutboundDup), `outbound_connect_failed` (→ TerminatedState), `release_outbound_connection` (OutboundUni → OutboundIdle → terminate, OutboundDup(Expired) → terminate, OutboundDup(Ticking) → InboundIdle, Duplex → InboundState), `include_inbound_connection` (accept phase with hard-limit check, overwrite of ReservedOutbound/Unnegotiated/Terminating/Terminated), `inbound_handshake_done` (Unnegotiated Inbound → InboundIdle), `release_inbound_connection` (InboundIdle → Terminating CommitTr, OutboundDup(Ticking) → Expired KeepTr, Duplex → OutboundDup KeepTr), `promoted_to_warm_remote` (InboundIdle → InboundState, OutboundDup → DuplexSt, OutboundIdle(Duplex) → InboundState, idempotent), `demoted_to_cold_remote` (InboundState → InboundIdle, Duplex → OutboundDup(Ticking), idempotent). Timeout helpers: `responder_timeout_expired`, `time_wait_expired`. Cleanup: `remove_terminated`, `mark_terminating`. Pruning: `maybe_prune` selects idle/terminated connections when inbound count exceeds hard limit. `prune_for_inbound(needed)` proactively evicts idle inbound and terminated connections before hard-limit rejection in `include_inbound_connection`, selecting terminated-first then newest-idle candidates. `AcquireOutboundResult` (Fresh/Reused/Disconnected), `ReleaseOutboundResult` (DemotedToColdLocal/Noop/Error). `counters()` folds `connection_state_to_counters()` over all connections for accurate full/duplex/uni/in/out counts (upstream `connectionManagerCounters`). 55 inline tests covering all operations, full outbound/inbound lifecycles, duplex reuse lifecycle, overwrite races, hard-limit enforcement, counter accuracy, timeout transitions, cleanup, prune-for-inbound eviction, and counters aggregation.
- Connection manager timeout scheduling is now wired into the pure CM state: each `ConnectionEntry` tracks responder and time-wait deadlines, and `timeout_tick(now)` advances timeout-driven transitions (`OutboundDup(Ticking)` → `Expired`, `Terminating` → `Terminated` → removal) and returns follow-up `CmAction`s for runtime execution.
- Diffusion-layer types (`diffusion.rs`): pure type module implementing upstream `Ouroboros.Network.Mux` and `Ouroboros.Network.ConnectionHandler` types. `TemperatureBundle<T>` (hot/warm/established) with `map`/`map_with_temp`/`iter`. `ProtocolTemperature` (Hot/Warm/Established). `MiniProtocolStart` (StartEagerly/StartOnDemand/StartOnDemandAny) — upstream `StartOnDemandOrEagerly`. `MiniProtocolLimits` (`maximum_ingress_queue`, default 2MB) — upstream `MiniProtocolLimits`. `MiniProtocolDescriptor` (num, temperature, start_mode, limits) — upstream `MiniProtocolInfo`. `OuroborosBundle` type alias = `TemperatureBundle<Vec<MiniProtocolDescriptor>>`. `ntn_ouroboros_bundle()` (ChainSync+BlockFetch hot, TxSubmission+KeepAlive warm, PeerSharing established). `ntc_ouroboros_bundle()` (LocalTxSubmission+LocalStateQuery+LocalTxMonitor established). `ControlMessage` (Continue/Quiesce/Terminate) — per-temperature flow control. `MuxMode` (InitiatorMode/ResponderMode/InitiatorResponderMode) — upstream `Mode`. `RateLimitDecision` (NoDelay/SoftDelay/HardLimit) with `rate_limit_decision()` — upstream `runConnectionRateLimits`. `ErrorCommand` (ShutdownConnection/ShutdownAndSuspend/Ignore), `RethrowPolicy` (RethrowException/AbsorbException), `ErrorPolicyResult`. `PeerConnectionHandle` (conn_id, data_flow, control: TemperatureBundle<ControlMessage>, peer_sharing_enabled) — upstream `PeerConnectionHandle`. `PeerStateAction` (EstablishConnection/ActivateConnection/DeactivateConnection/CloseConnection) — pure governor→runtime bridge. `RepromoteDelay` (ShortDelay 10s / LongDelay 200s). 28 inline tests.
- Peer-state action bridge helpers (`peer_state_actions.rs`) are now in place: `PeerStateActions` runtime executor trait plus governor-action translation helpers (`governor_action_to_peer_state_action`, `governor_actions_to_peer_state_actions`) that preserve ordering and drop non-connection governor actions. This is the crate-owned seam used by runtime integration work in `node`.
