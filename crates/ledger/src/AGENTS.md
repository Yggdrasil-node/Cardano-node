# Guidance for shared ledger internals outside era-specific modules
Focus on core ledger plumbing shared across eras: CBOR codec, core types, and state integration surfaces.

## Scope
- `cbor.rs`, `types.rs`, generic ledger state helpers, and module wiring under `crates/ledger/src`.
- Boundaries between shared ledger infrastructure and `eras/` era-specific logic.

##  Rules *Non-Negotiable*
- Keep CBOR behavior deterministic and round-trip tested.
- Do not duplicate era-specific rules in shared modules.
- Maintain strong type wrappers for protocol-relevant identifiers (`SlotNo`, `BlockNo`, `HeaderHash`, `Point`, `TxId`).
- Public shared APIs MUST have Rustdocs when semantics are non-obvious.
- Stay true to the official type naming and terminology for node concepts, network protocols, and ledger types when possible.
- Always read the folder specific `**/AGENTS.md` files. They MUST stay current and MUST remain operational rather than long-form documentation. If the folder context is outdated, missing, or incorrect, update the relevant `AGENTS.md` file.

## Official Upstream References *Always research references and add or update links as needed*
- [Ledger repository root](https://github.com/IntersectMBO/cardano-ledger)
- [Binary serialization library (`cardano-ledger-binary`)](https://github.com/IntersectMBO/cardano-ledger/tree/master/libs/cardano-ledger-binary)
- [Core ledger library (`cardano-ledger-core`)](https://github.com/IntersectMBO/cardano-ledger/tree/master/libs/cardano-ledger-core)
- [Shelley ledger support (`cardano-ledger-shelley`)](https://github.com/IntersectMBO/cardano-ledger/tree/master/eras/shelley/impl)
- [Protocol parameters in ledger](https://github.com/IntersectMBO/cardano-ledger/tree/master/libs/cardano-protocol-tpraos)
- [Formal ledger specification (Agda)](https://github.com/IntersectMBO/formal-ledger-specifications)
- [Published formal spec site](https://intersectmbo.github.io/formal-ledger-specifications/site)
- [Ledger Haddock documentation](https://cardano-ledger.cardano.intersectmbo.org/)

## Current Phase
- Hand-rolled CBOR encoder/decoder supports major Cardano-required primitives including signed integers (`integer()`).
- CBOR decoder supports both definite- and indefinite-length encoding (RFC 8949 §3.2.1): `array_begin()`/`map_begin()` return `Option<u64>` (None = indefinite), `is_break()`/`consume_break()` for break stop-code detection, `bytes_owned()`/`text_owned()` concatenate indefinite-length chunks, and `skip()` handles all indefinite containers. `array_or_set()` transparently handles CBOR tag 258 (finite set) wrapping: peeks for tag, if 258 consumes it, then delegates to `array()`. All set-typed CBOR fields across all 6 eras (Shelley–Conway) and WitnessSet use `array_or_set()` for inputs, certificates, required_signers, collateral, reference_inputs, voting_procedures, and all WitnessSet arrays (upstream `decCBOR` for `Set a`). `PlutusData` decode uses `array_begin()`/`map_begin()`/`bytes_owned()` so user-provided script data with indefinite-length CBOR is accepted.
- Shared typed core identifiers and point/nonce primitives are in place.
- Shared transaction plumbing now includes `compute_tx_id` plus submitted-transaction wrappers for Shelley-family and Alonzo-family wire shapes, with `MultiEraSubmittedTx` as the era-directed decode boundary for node-to-node relay work.
- Credential and address types landed: `StakeCredential` (key-hash/script-hash), `RewardAccount` (29-byte structured), `Address` (Base/Enterprise/Pointer/Reward/Byron variants), with CBOR codecs and variable-length natural encoding for pointer addresses. Deep validation now rejects invalid Shelley network ids, rejects pointer addresses with trailing bytes after the 3-variable-nat pointer, and exposes strict Byron bootstrap-address validation with outer `[tag24(payload), crc32]` structure and CRC32 verification via `Address::validate_bytes()` / `Address::validate()`.
- Certificate hierarchy landed in `types.rs`: `Anchor` (moved from conway.rs), `UnitInterval` (tag-30 rational), `Relay` (3-variant), `PoolMetadata`, `PoolParams` (9-field inline group), `DRep` (4-variant Conway), `DCert` (19-variant flat enum covering Shelley tags 0–5 and Conway tags 7–18), all with CBOR codecs in `cbor.rs`.
- `LedgerState` owns dual UTxO sets: `ShelleyUtxo` (legacy, authoritative for Shelley blocks) and `MultiEraUtxo` (generalized). It also carries explicit `PoolState`, `StakeCredentials`, `CommitteeState`, `DrepState`, `RewardAccounts`, and `current_epoch: EpochNo` containers plus `LedgerStateSnapshot` read-only views and `LedgerStateCheckpoint` restorable checkpoints. `apply_block()` dispatches per era with atomic block application; Byron blocks now perform real UTxO transitions via `apply_byron_block()` which decodes `ByronTx` from transaction body bytes, validates input existence and non-negative implicit fee (`consumed >= produced`), and applies UTxO updates atomically on `multi_era_utxo`. Byron inputs (`ByronTxIn` with u32 index) are converted to `ShelleyTxIn` (u16 index) for unified UTxO set storage; Byron outputs are stored as `MultiEraTxOut::Shelley` since their shape is identical to `ShelleyTxOut`. Shelley block/submitted-tx application now rebuilds `multi_era_utxo` from the authoritative `ShelleyUtxo` after each successful transition so Shelley outputs survive the Shelley→Allegra handoff. Transient pending Shelley genesis bundles can also be attached to `LedgerState`; initial funds and static stake delegations both activate once on the first Shelley-family block and are intentionally omitted from checkpoint CBOR so persisted snapshots remain pure ledger state. `RegisteredPool::relay_access_points()` and `PoolState::relay_access_points()` now expose only directly dialable single-host relay forms with declared ports so higher layers can consume immutable-ledger peers without re-implementing relay decoding.
- `LedgerStateCheckpoint`, `LedgerState`, the state-container wrappers, and both UTxO views now have deterministic CBOR round-trip support so higher layers can persist typed recovery checkpoints instead of opaque ad hoc bytes.
- `utxo.rs` module landed: `MultiEraTxOut` enum (Shelley/Mary/Alonzo/Babbage), `MultiEraUtxo` with per-era apply methods including Byron (`apply_byron_tx()` with implicit fee validation and ByronTxIn→ShelleyTxIn conversion), TTL, validity interval start, coin preservation, and multi-asset preservation checks. `validate_nonempty()` enforces upstream `InputSetEmptyUTxO` only — empty outputs are permitted (CDDL allows `[* transaction_output]`; upstream has no `OutputSetEmptyUTxO`; Byron retains its own inline empty-output rejection). All six per-era value-preservation functions (Shelley through Conway) now receive `deposits` and `refunds` from `CertBalanceAdjustment` and enforce the upstream equation: `consumed + withdrawal_total + refunds = produced + fee + deposits [+ donation for Conway]` (upstream reference: `Cardano.Ledger.Shelley.Rules.Utxo` / `Cardano.Ledger.Conway.Rules.Utxo`). Conway value preservation additionally includes `treasury_donation` on the produced side. `validate_no_ada_in_mint()` enforces the formal spec predicate `adaPolicy ∉ supp mint tx` (`TriesToForgeADA`) — rejects mint maps containing the ADA policy ID `[0u8; 28]`; wired into all 4 mint-capable era UTxO apply functions (Mary/Alonzo/Babbage/Conway) covering both block-apply and submitted-tx paths.
- Era-specific structures live under `eras/`; all eras Shelley through Conway are implemented. Shared layer should stay lightweight and stable.
- `PlutusData` is integrated into: `Redeemer.data` (typed payload), `DatumOption::Inline` (typed inline datum with tag-24 double encoding), `ShelleyWitnessSet.plutus_data` (typed `Vec<PlutusData>`).
- `protocol_params.rs`: `ProtocolParameters` struct with all Shelley-through-Conway parameter fields, `Default` (Shelley mainnet), `alonzo_defaults()`, `min_lovelace_for_utxo()`, full CBOR map-based round-trip codec. `ProtocolParameterUpdate` is now a typed sparse delta with matching CBOR-map keys, `is_empty()`, and `ProtocolParameters::apply_update()`. Wired into `LedgerState` as `Option<ProtocolParameters>` (array element 10, backward-compatible with 9-element legacy).
- `fees.rs`: Fee calculation and validation — `min_fee_linear()`, `script_fee()`, `total_min_fee()`, `validate_fee()`, `validate_tx_ex_units()`, `validate_tx_size()`. `script_fee()` mirrors upstream `txscriptfee`: sum memory and step rational costs first, then apply one ceiling to the total. Conway tiered reference-script fees: `tier_ref_script_fee()` (exponential tiered pricing per upstream `tierRefScriptFee` with multiplier 1.2 / stride 25,600), `conway_total_min_fee()` (base Alonzo fee + ref-script fee per upstream `getConwayMinFeeTx`), `validate_conway_fee()`. All Alonzo+ block-apply and submitted-tx paths route through `validate_conway_fee()` via `validate_alonzo_plus_tx()`, passing `ref_scripts_size` (0 for pre-Conway eras, computed from `total_ref_scripts_size()` for Conway).
- `native_script.rs`: Native (timelock) script evaluator — `evaluate_native_script()`, `native_script_hash()` (Blake2b-224 with language tag prefix), `NativeScriptContext`.
- `collateral.rs`: Alonzo+ collateral validation — `validate_collateral()` checks count limit, UTxO lookup, VKey-locked address enforcement (rejects script-locked collateral per upstream `validateScriptsNotPaidUTxO`), non-ADA rejection (Alonzo) / net-ADA-only (Babbage+), `total_collateral` field consistency, and percentage-of-fee sufficiency. Mandatory collateral for phase-2 scripts (`MissingCollateralForScripts`) enforced in `validate_alonzo_plus_tx()`. Upstream `feesOK` parity: collateral content validation is gated by redeemer presence (`txrdmrs ≠ ∅`) and is skipped when no redeemers are present even if collateral inputs are supplied. Babbage/Conway parity: `max_collateral_inputs` is still enforced as a standalone UTXO check regardless of redeemers (`validateTooManyCollateralInputs`), wired via era-aware call-site flagging in `validate_alonzo_plus_tx()`.
- `min_utxo.rs`: Minimum UTxO output validation — `validate_min_utxo()`, `validate_all_outputs_min_utxo()` using protocol params.
- `witnesses.rs`: VKey witness sufficiency and Ed25519 signature verification — `validate_vkey_witnesses()`, `verify_vkey_signatures()` (real Ed25519 verification against tx body hash using `yggdrasil_crypto::ed25519`), `verify_bootstrap_witnesses()` (Ed25519 verification for bootstrap witnesses plus CBOR-map validation of Byron address attributes), `vkey_hash()` (Blake2b-224), `witness_vkey_hash_set()`. Helper functions for collecting required VKey hashes and script hashes from spending inputs, certificates, withdrawals, and Conway voting procedures — both Shelley UTxO and multi-era UTxO variants. All per-era `apply_block()` inner loops now call `validate_witnesses_if_present()` to enforce both VKey witness hash sufficiency and witness signature validity when witness bytes are provided.
- `witnesses.rs` now also exposes `required_script_hashes_from_mint()` so mint policy IDs participate in required-script collection for Mary, Alonzo, Babbage, and Conway block application.
- `plutus_validation.rs`: trait-based Plutus phase-2 seam for the ledger crate — `PlutusEvaluator`, `PlutusScriptEval`, `PlutusVersion`, `ScriptPurpose`, `plutus_script_hash()`, and `validate_plutus_scripts()`. Spending-purpose Plutus resolution now uses the staged `MultiEraUtxo` to recover the spent output's script hash from the payment credential and the datum from either witness-set datum hashes (Alonzo-style) or inline datums (Babbage/Conway-style). Full `ScriptContext` / `TxInfo` construction is implemented in `node/src/plutus_eval.rs::build_tx_info` for all three Plutus versions (V1: 10 fields, V2: 12 fields with reference inputs, V3: 16 fields with Conway governance data). Cross-checked by `tx_info_v1_has_10_fields` / `tx_info_v2_has_12_fields_with_reference_inputs` / `tx_info_v3_has_16_fields` plus version-gating tests (`tx_info_v1_rejects_reference_inputs`, etc.). Use this path instead of adding a direct dependency from `ledger` to `plutus`.
- `plutus_validation.rs` `TxContext` now carries `protocol_version: Option<(u64, u64)>` for version-dependent ScriptContext encoding. All 6 TxContext construction sites (3 submitted-tx + 3 block-apply) set it from `ProtocolParameters.protocol_version`. Conway PV9 (bootstrap phase) `hardforkConwayBootstrapPhase` deposit omission is implemented in the node evaluator (see `node/src/AGENTS.md`). Reference: `Cardano.Ledger.Conway.TxInfo — transTxCert`, upstream bug #4863.
- `plutus_validation.rs` carries the active ordered protocol-parameter cost-model array on each `PlutusScriptEval` so the injected evaluator uses the epoch-specific model. The node evaluator is responsible for converting that array through the correct per-language upstream order table before CEK evaluation.
- `plutus_validation.rs` now enforces upstream `NoCostModel` collect errors before CEK evaluation: when a required Plutus V1/V2/V3 script is present and `ProtocolParameters.cost_models` exists but lacks the corresponding language key (0/1/2), `validate_plutus_scripts()` returns `LedgerError::NoCostModel { language }`. When `cost_models` is absent entirely, the check is soft-skipped for partially configured sync/replay contexts.
- `plutus_validation.rs` now enforces the required-redeemer half of upstream `hasExactSetOfRedeemers`: after resolving the required Plutus-backed purposes from spending inputs, mint policies, certificates, withdrawals, Conway voters, and proposal guardrails, `validate_plutus_scripts()` rejects any missing redeemer pointer with `LedgerError::MissingRedeemer { hash, purpose }` before cost-model checks or CEK evaluation. `validate_no_extra_redeemers()` now runs as a standalone Phase-1 UTXOW check in all three block-apply methods (Alonzo/Babbage/Conway) BEFORE the `is_valid` dispatching, matching upstream `hasExactSetOfRedeemers` placement in `alonzoUtxowTransition`. Regression coverage exercises both block and submitted-tx minting paths, plus `is_valid=false` and Babbage/Conway block-apply paths.
- `plutus_validation.rs` script-integrity hashing now matches upstream `mkScriptIntegrity`: `compute_script_data_hash()` / `validate_script_data_hash()` derive language views from the Plutus scripts actually used by the transaction, not every provided script. Babbage/Conway resolve needed reference-input script refs through the live `MultiEraUtxo`, and era call sites keep the upstream redeemer encoding split: Alonzo and Babbage use the legacy redeemer array encoding, while Conway uses map-format redeemers. `script_integrity_needed()` implements the upstream triple-null guard: a hash is required when ANY of (redeemers, datums, langViews) is non-empty — upstream returns `SNothing` only when all three are null (`Cardano.Ledger.Alonzo.Tx.mkScriptIntegrity`). Datum maps for spending-datum resolution and `missingRequiredDatums` use original witness `TxDats` item bytes when available, matching upstream memoized `TxDats` hashes and avoiding canonical re-encoding drift. Bidirectional enforcement: `MissingRequiredScriptIntegrityHash` when any component is non-empty but body omits `script_data_hash`; `UnexpectedScriptIntegrityHash` when body declares hash but all three components are empty. PV-aware error split: at PV >= 11 hash mismatches return `ScriptIntegrityHashMismatch` instead of `PPViewHashesDontMatch` (upstream `Cardano.Ledger.Conway.Rules.Utxow`).
- Per-era script validation now enforces required-script witness presence in both `apply_block()` and Alonzo-family `apply_submitted_tx()` paths: `validate_native_scripts_if_present()` evaluates required native scripts and `validate_required_script_witnesses()` enforces `MissingScriptWitness` when a required script hash is missing from both native and Plutus witness sources (including reference scripts for Babbage/Conway). Required-script collection covers script-hash payment credentials in spending inputs, certificates, withdrawals, mint policy IDs for mint-capable eras, and Conway voting/proposal guardrails hashes.
- `state.rs` now exposes `LedgerState::apply_block_validated()` as the evaluator-aware block-application entry point. Higher layers that have a Plutus CEK implementation should call this method and inject a `PlutusEvaluator`; `apply_block()` remains the no-evaluator convenience wrapper. `apply_block_validated()` auto-tracks block production via `record_block_producer()` for non-Byron blocks and, when an evaluator is provided, calls `validate_script_witnesses_well_formed()` and `validate_reference_scripts_well_formed()` in Alonzo+ eras (upstream `validateScriptsWellFormed` from `Cardano.Ledger.Alonzo.Rules.Utxos`).
- `witnesses.rs` script well-formedness: `validate_script_witnesses_well_formed()` and `validate_reference_scripts_well_formed()` call `PlutusEvaluator::is_script_well_formed(version, protocol_version, raw_plutus_binary)` to detect malformed Plutus scripts at admission time, returning `MalformedScriptWitnesses` / `MalformedReferenceScripts` errors. The bytes are raw `PlutusBinary` after ledger CBOR decoding; upstream `PlutusBinary` is `SerialisedScript`, a CBOR bytestring containing the Flat UPLC program, so evaluators must decode that inner bytestring deterministically instead of falling back between raw Flat and CBOR. Wired into all Alonzo+-era `apply_block_validated` paths.
- `utxo.rs` forecast validation: `validate_outside_forecast()` implements upstream `OutsideForecast` infrastructure from `Cardano.Ledger.Shelley.Rules.Utxo` — marked as upstream-equivalent no-op because `unsafeLinearExtendEpochInfo` makes the check always pass.
- `state.rs` network validation: `shelley_address_network_id()` extracts the network ID nibble from Shelley/reward-family address header bytes (types 0–7, 14, 15), returning `None` for Byron (type 8) and unused types. `validate_output_network_ids()`, `validate_withdrawal_network_ids()`, and `validate_tx_body_network_id()` implement the upstream `ShelleyUtxoPredFailure.WrongNetwork`, `WrongNetworkWithdrawal`, and `AlonzoUtxoPredFailure.WrongNetworkInTxBody` rules. All six per-era `apply_*_block()` methods call the output + withdrawal validators when `expected_network_id` is set; Alonzo/Babbage/Conway additionally validate the tx body `network_id` field. The same validation is also wired into all six per-era branches of `apply_submitted_tx()` so mempool admission enforces network-id checks. Babbage and Conway `apply_submitted_tx()` branches additionally validate reference inputs via `staged.validate_reference_inputs()` to reject transactions referencing non-existent UTxOs at admission time. `validate_reference_input_disjointness()` (upstream `disjointRefInputs`) is PV-gated via `disjoint_ref_inputs_enforced()`: enforced only when `pvMajor > 8 && pvMajor < 11` (PV 9–10, early Conway), relaxed at PV 11+ per upstream `Cardano.Ledger.Babbage.Rules.Utxo`.
- `state.rs` auxiliary-data validation now threads protocol version into `validate_auxiliary_data()`. From protocol version > (2, 0) onward (Allegra+ soft fork), `validate_auxiliary_data_metadata_sizes()` enforces upstream `validMetadatum` string/bytes limits: all transaction-metadatum bytes and text values, including nested array/map entries, must be <= 64 bytes or the tx is rejected with `InvalidMetadata`.
- Conway block application now validates `current_treasury_value` against `LedgerState.accounting.treasury` when supplied (inside `IsValid True` branch only — upstream `validateTreasuryValue` placement), validates `total_ref_scripts_size` limit inside `IsValid True` branch only (upstream `conwayLedgerTransitionTRC` placement — `is_valid=false` txs skip both checks), validates proposal deposits against `ProtocolParameters.gov_action_deposit` when present, rejects `TreasuryWithdrawals` proposals whose requested withdrawals sum to zero, rejects `UpdateCommittee` proposals that add and remove the same cold credential via `ConflictingCommitteeUpdate`, rejects `UpdateCommittee` member additions whose expiration epoch is not strictly after `LedgerState.current_epoch` via `ExpirationEpochTooSmall`, rejects invalid previous governance-action references via `InvalidPrevGovActionId` both for same-transaction self and forward references and for cross-transaction references whose stored parent action is missing or has the wrong governance purpose, rejects votes on stored governance actions whose tracked `expires_after` is earlier than `LedgerState.current_epoch` via `VotingOnExpiredGovAction`, and rejects empty typed `ParameterChange.protocol_param_update` payloads via `MalformedProposal`. `InvalidGuardrailsScriptHash` validation now enforces that `ParameterChange` and `TreasuryWithdrawals` proposals carry a `guardrails_script_hash` matching the constitution's guardrails script hash in `EnactState` (upstream `checkGuardrailsScriptHash` from `Cardano.Ledger.Conway.Rules.Gov`). `ProposalReturnAccountDoesNotExist` and `TreasuryWithdrawalReturnAccountsDoNotExist` now use upstream-named error variants for unregistered proposal return addresses and treasury withdrawal target accounts (previously generic `RewardAccountNotRegistered`). Enactment now applies typed parameter updates directly to `LedgerState.protocol_params` through `ProtocolParameters::apply_update()` and records the lineage root.
- Conway hard-fork proposals now enforce upstream `pvCanFollow` sequencing via `ProposalCantFollow`: the first `HardForkInitiation` is compared against `ProtocolParameters.protocol_version`, and chained hard-fork proposals are compared against the referenced stored hard-fork action's target protocol version.
- `ProtocolParameters` now carries `protocol_version`, seeded from Shelley genesis `protocolParams.protocolVersion` and encoded at the upstream protocol-parameter key 14. Protocol-parameter CBOR decode remains backward-compatible with older local checkpoints that wrote `min_utxo_value` at key 14 before protocol-version tracking was added.
- Conway bootstrap GOV checks are now enforced when `ProtocolParameters.protocol_version` has major version 9: proposals are limited to bootstrap actions (`ParameterChange`, `HardForkInitiation`, `InfoAction`), DReps may only vote on `InfoAction`, and committee/SPO voters may only vote on bootstrap actions. Unit tests now cover the complete bootstrap matrix: disallowed non-bootstrap paths (proposal + DRep/committee/SPO votes) and allowed bootstrap paths for all three proposal types (`ParameterChange`, `HardForkInitiation` implicit, `InfoAction`), DRep `InfoAction` votes, and committee/SPO votes on all three bootstrap actions (`HardForkInitiation`, `ParameterChange`, `InfoAction`). Post-bootstrap voter permission unit tests cover the SPO security-group `ParameterChange` boundary (allowed when update touches security-group params like `min_fee_a`/`max_block_body_size`, rejected when only non-security params like `key_deposit`), SPO rejection on `NewConstitution`, and committee rejection on `NoConfidence`. `conway_modified_pparam_groups()` parameter-group classification now has comprehensive direct unit tests and is fully aligned with upstream `ConwayPParams` THKD type-level annotations: `min_fee_a`/`min_fee_b`/`coins_per_utxo_byte`/`min_fee_ref_script_cost_per_byte` = economic+security, `max_block_body_size`/`max_tx_size`/`max_block_header_size`/`max_block_ex_units`/`max_val_size` = network+security, `max_tx_ex_units`/`max_collateral_inputs` = network (no SPO), `key_deposit`/`pool_deposit`/`rho`/`tau`/`min_pool_cost`/`price_mem`/`price_step` = economic (no SPO), `e_max`/`n_opt`/`a0`/`collateral_percentage`/`cost_models` = technical (no SPO), `gov_action_deposit` = gov+security, remaining gov params = gov (no SPO). DRep threshold dispatch includes security-only→`None` and single-economic-group edge cases. This remains a bounded slice until on-chain protocol-version evolution is modeled beyond the genesis-seeded baseline.
- Bootstrap-phase DRep delegation parity: `CertificateValidationContext` carries `bootstrap_phase: bool` (derived from `conway_bootstrap_phase()` checking PV == 9). `delegate_drep()` skips `checkDRepRegistered` during bootstrap (upstream `preserveIncorrectDelegation` / `hardforkConwayBootstrapPhase` from `Cardano.Ledger.Conway.Rules.Deleg`). `apply_conway_votes()` and `touch_drep_activity_for_certs()` skip dormant epoch subtraction during bootstrap (upstream `computeDRepExpiryVersioned` from `Cardano.Ledger.Conway.Rules.GovCert`). HARDFORK PV9→10 transition now applies `updateDRepDelegations` parity via `cleanup_dangling_drep_delegations()` in epoch-boundary ratification enactment: dangling delegations to non-existent non-builtin DReps are removed; PV10→11 does not run this cleanup (upstream `populateVRFKeyHashes` branch).
- Conway post-bootstrap withdrawal gating parity: `validate_withdrawals_delegated()` runs in both `apply_conway_block()` and `apply_submitted_tx()` before certificate processing, using the pre-CERTS stake credential state. For key-hash reward accounts, post-bootstrap withdrawals require an existing DRep delegation or return `WithdrawalNotDelegatedToDRep`; bootstrap phase skips the check and script-hash reward accounts are excluded, matching upstream `validateWithdrawalsDelegated` / `ConwayWdrlNotDelegatedToDRep`.
- Conway malformed-proposal validation now includes bounded `ParameterChange.protocol_param_update` guardrails beyond non-empty checks: UnitInterval fields must satisfy rational bounds, threshold bundles (`PoolVotingThresholds`/`DRepVotingThresholds`) are range-checked, selected positive-only fields reject zero, protocol-version updates inside `ParameterChange` are rejected (Conway requires protocol-version changes through `HardForkInitiation`), and tx/block-size constraints reject `max_tx_size > max_block_body_size` against either the proposed update pair or current protocol parameters. Parameter-change ratification now classifies typed updates by modified protocol-parameter groups and applies thresholds accordingly: DRep parameter-change threshold uses the maximum of modified DRep groups (`network/economic/technical/gov`) and SPO voting/threshold gating is enabled only when the update touches an SPO security group. No-confidence actions now use `motion_no_confidence` thresholds for both DRep and SPO ratification paths (instead of committee-normal thresholds), and `UpdateCommittee` threshold routing is now state-aware (`committee_normal` when at least one committee member is non-resigned, otherwise `committee_no_confidence`) for both DRep and SPO paths, including resign-only committee states. The local purpose grouping matches upstream Conway GOV roots: `ParameterChange`, `HardForkInitiation`, and `NewConstitution` each require the same constructor family, while `NoConfidence` and `UpdateCommittee` share the committee-purpose lineage. Stored `GovernanceActionState` now optionally carries `proposed_in` and `expires_after` metadata derived from `ProtocolParameters.gov_action_lifetime`; legacy checkpoints without that metadata still decode with `None`. Proposal and TreasuryWithdrawals reward-account network ids are validated against `LedgerState.expected_network_id` when configured, Conway voters are validated against the post-CERTS committee/DRep/pool state so same-tx committee hot-key authorization and DRep registration count for `VotersDoNotExist`, accepted proposals persist in `LedgerState.governance_actions`, accepted votes are recorded into the stored action state, and DRep unregistration removes stored DRep votes. `UpdateCommittee` proposal validation now also enforces member expiration upper bounds from `ProtocolParameters.committee_term_limit` (in addition to `current_epoch` lower-bound checks), and hard-fork proposals now require an available baseline protocol version for `prev_action_id = None` and enforce `pvCanFollow` against the current protocol version when `prev_action_id` references the enacted hard-fork root. Regression coverage now also asserts missing-baseline rejection for the enacted-root hard-fork reference path. Full live protocol-version/enact-state tracking still needs richer Conway governance state.
- `stake.rs`: Stake distribution snapshots and epoch-boundary snapshot rotation — `IndividualStake`, `Delegations`, `StakeSnapshot`, `StakeSnapshots` (three-snapshot ring: mark/set/go + fee pot), `PoolStakeDistribution`, `compute_stake_snapshot()`, `compute_drep_stake_distribution()`, `StakeSnapshots::rotate()`, `StakeSnapshot::pool_stake_distribution()`. All types have CBOR round-trip codecs. `compute_drep_stake_distribution()` aggregates per-DRep delegated lovelace from `StakeSnapshot` stake amounts and credential-level DRep delegations for use in ratification vote tallying.
- `rewards.rs`: Epoch reward calculation (Shelley spec Section 10) — `RewardParams`, `EpochRewardPot`, `PoolRewardBreakdown`, `EpochRewardDistribution` (carries `delta_reserves` for reserves-only accounting), `mul_rational()`, `floor_mul_div()`, `compute_epoch_reward_pot()`, `max_pool_reward()`, `compute_pool_reward()`, `compute_epoch_rewards()`. Uses exact U256 rational arithmetic with single-floor-at-end semantics matching upstream `rationalToCoinViaFloor`. `U256` (256-bit unsigned integer) with `widening_mul`, `add`, `mul_u128`, `div_u128`, and `u256_div_floor` for overflow-free computation. `gcd128()` helper for rational GCD reduction. All rational pairs (σ'/s'/diff) are GCD-reduced before U256 multiplication chains to prevent intermediate overflow.
- `epoch_boundary.rs`: Epoch boundary orchestration (NEWEPOCH / RUPD / MIR / SNAP / RATIFY) — `apply_epoch_boundary()` follows the upstream `NewEpoch` rule where RUPD (reward computation and distribution from fees and pre-rotation `go` snapshot) happens BEFORE SNAP (new mark snapshot from post-reward state, then rotation). MIR application (`apply_mir_at_epoch_boundary()`) runs between RUPD and SNAP, matching upstream ordering (reference: `Cardano.Ledger.Shelley.Rules.Mir`): filters accumulated `InstantaneousRewards` to registered reward accounts, checks pot sufficiency with all-or-nothing semantics (if either reserves or treasury is insufficient, neither pot pays), credits reward accounts with merged per-credential amounts, debits pots, applies `SendToOppositePot` delta transfers regardless of reward outcome, and always clears IR state. Reserves are decremented only by `delta_reserves` (monetary expansion via ρ), not by fees. Also performs pool retirement + deposit refunds, governance action expiry + deposit refunds, ratification + enactment of accepted governance actions, DRep inactivity detection, and treasury/reserves accounting update. `retire_pools_with_refunds()` captures reward accounts before removing pools. `remove_expired_governance_actions()` prunes proposals whose `expires_after` epoch has passed and refunds deposits to registered return accounts (matching upstream Conway EPOCH rule). `ratify_and_enact()` iterates surviving governance actions in upstream `actionPriority` order, re-reads voting thresholds (`drep_voting_thresholds`, `pool_voting_thresholds`, `min_committee_size`, `is_bootstrap_phase`) from `ledger.protocol_params()` on each iteration so that a ParameterChange enactment updates thresholds for subsequent proposals within the same epoch boundary (matching upstream `ratifyTransition` which reads `rs ^. rsEnactStateL . ensCurPParamsL` per-proposal), evaluates each against committee/DRep/SPO acceptance predicates via `ratify_action()`, enacts accepted actions via `LedgerState::enact_action()`, removes enacted actions from the pending set, refunds all removed-action deposits to return accounts (matching upstream `returnProposalDeposits`), and performs lineage subtree pruning via `remove_lineage_conflicting_proposals()` (matching upstream `proposalsApplyEnactment`). Unclaimed deposits (unregistered return accounts) are credited to treasury. `RatifyAndEnactResult` struct carries enacted IDs, outcomes, enacted deposit refunds, subtree-removed IDs, subtree deposit refunds, and unclaimed deposits. `gov_action_prev_id()` extracts `prev_action_id` from lineage-bearing `GovAction` variants. When protocol parameters are present but the Conway voting-threshold fields are unset, ratification falls back to the built-in `PoolVotingThresholds::default()` / `DRepVotingThresholds::default()` values instead of silently skipping governance actions. DRep inactivity step computes the set of DReps exceeding the `drep_activity` window (upstream `drepExpiry`); inactive DReps remain registered but are excluded from ratification quorum calculations. Epoch-boundary ratification tests now cover all 7 governance action types: InfoAction (never ratified — upstream NoVotingThreshold for all three voter roles), HardFork (with CC+DRep votes), NewConstitution (with CC+DRep votes), UpdateCommittee (resign-only committee, min-committee-size gating, elected-committee-without-votes rejection, meets-min-size with committee vote), NoConfidence (ratified with resigned CC removes committee and resets quorum, not ratified without DRep/SPO approval), TreasuryWithdrawals (ratified credits reward accounts from treasury, not ratified without votes), ParameterChange (ratified applies protocol param update, not ratified without votes). Also covers mixed ratification+expiry, no-pending-actions, DRep inactivity, enacted deposit refund (registered + unregistered accounts), sibling proposal pruning, transitive subtree pruning, cross-purpose non-pruning. Returns `EpochBoundaryEvent` summary with governance_actions_expired, governance_deposit_refunds, expired_gov_action_ids, dreps_expired, governance_actions_enacted, enacted_gov_action_ids, enact_outcomes, enacted_deposit_refunds, removed_due_to_enactment, removed_due_to_enactment_deposit_refunds, unclaimed_governance_deposits.
- Shelley PPUP (protocol-parameter updates) is wired in `state.rs` + `epoch_boundary.rs`: Shelley/Allegra/Mary/Alonzo/Babbage block application now collects `ShelleyUpdate` proposals into `LedgerState.pending_pparam_updates`; epoch transitions call `apply_pending_pparam_updates(new_epoch)` before reward math, requiring quorum (majority of active `gen_delegs`) on an identical `ProtocolParameterUpdate`. Alonzo/Babbage `is_valid=false` transactions are skipped during proposal collection (upstream `alonzoEvalScriptsTxInvalid` returns `pure pup`). `EpochBoundaryEvent` includes `pparam_updates_applied` for observability.
- PPUP proposal validation (upstream `Cardano.Ledger.Shelley.Rules.Ppup`): `validate_ppup_proposal()` enforces `NonGenesisUpdatePPUP` (proposer must be in `gen_delegs`), `PPUpdateWrongEpoch` (target epoch must match voting-period with optional `PpupSlotContext` slot-of-no-return check, or relaxed current/current+1), and `PVCannotFollowPPUP` (proposed protocol version must satisfy `pv_can_follow` — major+1/minor=0 or same major/minor+1). Wired into all 5 block-apply paths (Shelley through Babbage). `pv_can_follow()` public free function. 18 PPUP validation tests.
- `state.rs` DRep activity tracking: `RegisteredDrep` carries `last_active_epoch: Option<EpochNo>` (3-element CBOR array, backward-compatible with legacy 2-element). `new_active()`, `last_active_epoch()`, `touch_activity()` methods. Activity is touched on DRep registration, DRep update (via `touch_drep_activity_for_certs()`), and vote casting (via `apply_conway_votes()`). `DrepState::inactive_dreps()` returns DReps whose `last_active_epoch + drep_activity < epoch`. Dormant epoch tracking (upstream `numDormantEpochs`): `LedgerState.num_dormant_epochs` (element 22, 23-field CBOR, backward-compatible), incremented at epoch boundary when no governance proposals remain, reset to 0 when proposals exist (reference: `Cardano.Ledger.Conway.Rules.Epoch — updateNumDormantEpochs`). Per-tx `update_dormant_drep_expiries()` bumps all non-expired DReps' `last_active_epoch` by the dormant counter when a transaction contains proposals (reference: `Cardano.Ledger.Conway.Rules.Certs — updateDormantDRepExpiries`). `apply_conway_votes()` and `touch_drep_activity_for_certs()` accept `num_dormant_epochs` and subtract it from `current_epoch` when computing DRep activity touch points (reference: `computeDRepExpiry`, `updateVotingDRepExpiry`).
- `protocol_params.rs` now includes `drep_deposit: Option<u64>` (CDDL key 31) and `drep_activity: Option<u64>` (CDDL key 32) for Conway DRep governance parameters.
- `protocol_params.rs` now includes `d: Option<UnitInterval>` (CDDL key 12, decentralization parameter, Shelley–Alonzo) and `extra_entropy: Option<Nonce>` (CDDL key 13, extra entropy for epoch nonce evolution, Shelley–Alonzo) with full CBOR map round-trip codec, `ProtocolParameterUpdate` propagation, and `apply_update()` support. Both are naturally absent (`None`) in Babbage/Conway.
- `state.rs` deposit tracking: `DepositPot` (key_deposits, pool_deposits, drep_deposits, proposal_deposits) and `AccountingState` (treasury, reserves) with CBOR codecs. `proposal_deposits` field (upstream `oblProposal` in `Obligations`) tracks outstanding governance proposal deposits; `total()` matches upstream `sumObligation` across all four obligation categories. Conway block-apply and submitted-tx paths accumulate proposal deposits via `add_proposal_deposit()`; epoch-boundary reconciliation debits returned/expired/enacted proposal deposits via `return_proposal_deposit()`. Backward-compatible CBOR decode (3-or-4 element arrays). `LedgerState` now 23-field struct with deposit_pot, accounting, `utxos_donation`, `instantaneous_rewards`, `genesis_update_quorum`, `num_dormant_epochs`, and `blocks_made`. `utxos_donation` accumulates per-tx Conway `treasury_donation` via `accumulate_donation()` in both block-apply and submitted-tx paths (upstream `utxosDonationL` in `Cardano.Ledger.Conway.Rules.Utxos`); `flush_donations_to_treasury()` transfers the accumulated amount to treasury at epoch boundaries (upstream `Cardano.Ledger.Conway.Rules.Epoch`). CBOR backward-compatible decode (9/10/12/15/16/18/19/20/21/22/23-element arrays). `blocks_made` (element 23, upstream `NewEpochState.nesBcur`) tracks per-pool block production counts; `record_block_producer()` called automatically from `apply_block_validated()` for non-Byron blocks, `take_blocks_made()` clears at epoch boundary. `apply_certificates_and_withdrawals()` returns `CertBalanceAdjustment { withdrawal_total, total_deposits, total_refunds }` so callers can thread deposit/refund totals into per-era value preservation checks. Conway value preservation additionally includes proposal deposits (`totalTxDeposits = certDeposits + proposalDeposits`, upstream `Cardano.Ledger.Conway.TxInfo`): both `apply_conway_block` and `apply_conway_submitted_tx` compute `proposal_deposits` from `body.proposal_procedures` and add to `cert_adj.total_deposits` before calling `apply_conway_tx_withdrawals`. Conway withdrawal validation enforces exact-drain semantics (upstream `WithdrawalsNotInRewardsCERTS` from `Cardano.Ledger.Conway.Rules.Certs`). Conway certificate deposit validation enforces DELEG hardfork split semantics for key deposits (`IncorrectDepositDELEG` in bootstrap PV 9, `DepositIncorrectDELEG` post-bootstrap PV 10+), key refunds (`IncorrectKeyDepositRefund` bootstrap, `RefundIncorrectDELEG` post-bootstrap), plus `DrepIncorrectDeposit` (DRep deposit must match `ppDRepDeposit`) and `DrepIncorrectRefund` (DRep refund must match stored deposit). Deposit tracking covers all 19 DCert variants: `AccountRegistration` (key_deposit from protocol params), `AccountRegistrationDeposit` (explicit), `AccountUnregistration` (key refund), `AccountUnregistrationDeposit` (explicit refund), `PoolRegistration` (pool_deposit for new pools), `DrepRegistration` (explicit deposit), `DrepUnregistration` (explicit refund), plus all combined registration+delegation variants. `process_retirements(epoch)` on `PoolState` removes retiring pools.
- `state.rs` Conway-only pool-delegation registration check: `delegate_stake_credential()` accepts `check_pool_registered: bool`; ALL eras (Shelley through Conway) pass `true` for pool delegation, matching upstream `DelegateeNotRegisteredDELEG` in `Cardano.Ledger.Shelley.Rules.Deleg` which enforces pool registration for `DelegStakeTxCert` across all Shelley-through-Babbage eras. Conway uses `DelegateeStakePoolNotRegisteredDELEG` in `ConwayDELEG`.
- `state.rs` Conway `checkStakeKeyNotRegistered` enforcement: All four Conway registration cert variants (`AccountRegistrationDeposit` tag 7, `AccountRegistrationDelegationToStakePool` tag 9/11, `AccountRegistrationDelegationToDrep` tag 12, `AccountRegistrationDelegationToStakePoolAndDrep` tag 13) now reject already-registered credentials with `StakeCredentialAlreadyRegistered` when `ctx.is_conway`, matching upstream `StakeKeyRegisteredDELEG` from `Cardano.Ledger.Conway.Rules.Deleg`.
- `state.rs` Conway VRF key uniqueness: `PoolState::find_pool_by_vrf_key()` linear scan detects duplicate VRF keys across pools. `DCert::PoolRegistration` handler checks VRF uniqueness when `ctx.post_pv10` (PV > 10 only) — new registrations reject if VRF key is used by another pool, re-registrations allow the same pool's own VRF key. `VrfKeyAlreadyRegistered` error variant in `error.rs`. Conway bootstrap (PV 9/10) allows duplicate VRF keys, matching upstream `hardforkConwayDisallowDuplicatedVRFKeys pv = pvMajor pv > natVersion @10`. Reference: `Cardano.Ledger.Shelley.Rules.Pool`.
- `state.rs` BBODY running UTxO ref-script size at PV > 10: `apply_conway_block()` now computes `totalRefScriptSizeInBlock` with two paths matching upstream `Cardano.Ledger.Conway.Rules.Bbody`: PV ≤ 10 sums `txNonDistinctRefScriptsSize` per tx over static pre-block UTxO; PV > 10 uses a running `HashMap` overlay that accumulates tx outputs (valid tx → regular outputs, invalid tx → collateral return at `index = outputs.len()` per upstream `mkCollateralTxIn`) so subsequent txs see prior tx outputs for ref-script measurement.
- `state.rs` pool owner registration NOT enforced: the upstream POOL rule (`Cardano.Ledger.Shelley.Rules.Pool`) intentionally omits pool-owner registration checks. `PoolOwnerNotRegistered` error variant retained for CBOR backward compatibility but never produced. Pool registration with unregistered owners is accepted, matching upstream behavior.
- `epoch_boundary.rs` POOLREAP delegation cleanup: `retire_pools_with_refunds()` now calls `StakeCredentials::clear_pool_delegations()` after removing retired pools, clearing the `delegated_pool` reference for every credential that was delegating to a retired pool. Reference: upstream `removeStakePoolDelegations` in `Cardano.Ledger.Shelley.Rules.PoolReap`.
- `epoch_boundary.rs` `derive_pool_performance()` takes `d: UnitInterval` (decentralization parameter). When `d >= 0.8` (early Shelley), returns perf=1 for all block-producing pools. Reference: upstream `mkApparentPerformance` in `Cardano.Ledger.Shelley.Rewards`.
- `state.rs` future pool params staging (upstream `psFutureStakePoolParams`): `PoolState` now carries a `future_params: BTreeMap<PoolKeyHash, PoolParams>` map alongside `entries`. Pool re-registration via `register_with_deposit()` stages updated params in `future_params` instead of applying immediately, clears any pending retirement (upstream `psRetiring` deletion), and preserves the original deposit and current params. `adopt_future_params()` drains the staged map into `entries` at epoch boundary (called from SNAP in `epoch_boundary.rs` before `compute_stake_snapshot()`). `find_pool_by_vrf_key()` searches both `future_params` and `entries` (skipping entries with future overrides) for VRF key uniqueness. `process_retirements()` also clears `future_params` for pools being retired. CBOR format uses a map with key 0 (entries) and optional key 1 (future_params), detected via `peek_major()` for backward compatibility with the legacy bare-array format. Reference: upstream `PState.psFutureStakePoolParams`, SNAP rule in `Cardano.Ledger.Shelley.Rules.Snap`.
- `state.rs` Move Instantaneous Rewards (MIR): `InstantaneousRewards` struct (`ir_reserves`, `ir_treasury` per-credential maps, `delta_reserves`, `delta_treasury` signed pot-to-pot fields) with CBOR round-trip codec. `accumulate_mir_from_certs()` public function scans certificate lists for `MoveInstantaneousReward` entries (DCert tag 6, Shelley through Babbage only) and merges per-credential deltas into the IR state using Alonzo+ `unionWith (<>)` semantics. `SendToOppositePot` targets adjust signed delta fields while maintaining `delta_reserves + delta_treasury == 0` invariant. All 5 block-apply methods (Shelley through Babbage) and all 5 submitted-tx arms wire `accumulate_mir_from_certs()` after each successful staged commit. Alonzo/Babbage `is_valid=false` transactions are skipped during MIR accumulation (upstream `alonzoEvalScriptsTxInvalid` does not run DELEGS). Conway is excluded (no MIR). MIR genesis quorum validation (`validateMIRInsufficientGenesisSigs`): `genesis_update_quorum: u64` (element 21, default 5) from `ShelleyGenesis.updateQuorum`; `validate_mir_genesis_quorum_if_present()` and `validate_mir_genesis_quorum_typed()` enforce >= quorum genesis delegate witnesses on any tx with MIR certs; wired into all 5 block-apply + 5 submitted-tx paths (Shelley-Babbage); 8 quorum tests. MIR admission validation (`MirValidationContext`): all 7 upstream DELEG MIR checks enforced in `MoveInstantaneousReward` cert arm — `MIRCertificateTooLateinEpochDELEG` (timing: slot < firstSlot(nextEpoch) - stabilityWindow), `MIRNegativesNotCurrentlyAllowed` (pre-Alonzo negative deltas), `MIRProducesNegativeUpdate` (Alonzo+ combined map negatives), `InsufficientForInstantaneousRewardsDELEG` (pot balance), `MIRTransferNotCurrentlyAllowed` (pre-Alonzo SendToOppositePot), `MIRNegativeTransfer` (unsigned, inherently satisfied), `InsufficientForTransferDELEG` (transfer pot balance after MIR commitments); era-gated via `alonzo_mir_transfers` flag matching upstream `hardforkAlonzoAllowMIRTransfer`; wired at all 10 Shelley-Babbage call sites (5 block-apply + 5 submitted-tx); Conway passes `None` (MIR certs rejected as UnsupportedCertificate); 8 MIR validation tests. Reference: `Cardano.Ledger.Shelley.Rules.Deleg`.
- `state.rs` `dsFutureGenDelegs` parity: `GenesisDelegation` certificates now schedule into `future_gen_delegs` at `activation_slot = current_slot + stability_window` (when configured), duplicate delegate/VRF checks include both active and future maps (excluding the same genesis key), and scheduled entries are adopted into `gen_delegs` when `current_slot >= activation_slot` via `adopt_scheduled_genesis_delegations()` (wired in both block-apply and submitted-tx paths). Reference: `Cardano.Ledger.Shelley.Rules.Deleg` (`dsFutureGenDelegs`, `adoptGenesisDelegs`).
- CBOR `Decoder::peek_is_null()` added for nullable field support.
- `state.rs` governance enactment: `EnactState` struct tracks the current enacted constitution, committee quorum, and four purpose-lineage prev-action-ids (PParamUpdate, HardFork, Committee, Constitution) matching upstream `GovRelation`. `enact_gov_action()` free function implements the Conway ENACT rule for all seven `GovAction` variants: InfoAction (no effect), NewConstitution (replace constitution + lineage), NoConfidence (remove all committee + reset quorum + lineage), UpdateCommittee (add/remove members + set quorum + lineage), HardForkInitiation (set protocol_version + lineage), TreasuryWithdrawals (credit registered reward accounts from treasury), ParameterChange (apply protocol-parameter delta via `ProtocolParameters::apply_update` AND record lineage; previously documented as "opaque update deferred" but the apply path now covers all 33 PPU fields including cost models and Conway governance parameters). HardFork enactment now initializes protocol parameters when absent so `protocol_version` is always recorded after ratification. ENACT `UpdateCommittee` now mirrors upstream `updatedCommittee`: members are removed, then `members_to_add` are applied verbatim with no extra local expiration/term-limit filtering in ENACT (those checks belong to RATIFY/GOV validation). Returns `EnactOutcome` enum for informational reporting. `LedgerState` now carries `enact_state: EnactState` (16-element CBOR, backward-compatible with 15-element). `LedgerStateSnapshot` mirrors the field. `enacted_root()` on `EnactState` returns the enacted root GovActionId for a given `ConwayGovActionPurpose`. `validate_conway_proposals()` uses enacted-root semantics: `prev_action_id = None` is valid only when the enacted root for that purpose is also `None`; `prev_action_id = Some(id)` must match either the enacted root or a stored pending proposal of the same purpose. `NoConfidence` and `UpdateCommittee` share the `Committee` purpose group.
- `state.rs` ratification tally engine (Phase 6): `VoteTally` struct with `meets_threshold()` using cross-multiplication to avoid floats. `tally_committee_votes()` counts CC yes/no/abstain by equal-weight registered non-resigned members, takes `current_epoch` and filters expired members per upstream `ccVotesSatisfied` (`currentEpoch <= expirationEpoch`). `tally_drep_votes()` counts DRep votes weighted by delegated stake, excluding inactive DReps past `drep_activity` window (strict `<`: `last_active + activity < current_epoch` means DRep is inactive; at exact boundary `==` DRep is still ACTIVE); `AlwaysNoConfidence` delegated stake is always included in total; it counts as automatic Yes only for NoConfidence actions, and as denominator-only (implicit No) for all other action types (upstream `Cardano.Ledger.Conway.Rules.Ratify`). `AlwaysAbstain` is excluded from total. `tally_spo_votes()` counts SPO votes weighted by pool stake from `PoolStakeDistribution`; post-bootstrap non-voting pools use `default_stake_pool_vote()` (upstream `defaultStakePoolVote`) which derives the default vote from the pool's reward-account DRep delegation: `AlwaysAbstain` → Abstain, `AlwaysNoConfidence` → auto-Yes on NoConfidence actions else implicit No, all others → implicit No; HardFork non-voting is always implicit No regardless of delegation; bootstrap-phase non-voting is always Abstain. `drep_threshold_for_action()` / `spo_threshold_for_action()` map `ConwayGovActionPurpose` to per-type thresholds from `DRepVotingThresholds` / `PoolVotingThresholds`. `accepted_by_committee()` implements upstream `votingCommitteeThresholdInternal` + `committeeAccepted` per-action-type semantics: NoConfidence/UpdateCommittee → NoVotingAllowed (always passes); InfoAction → NoVotingThreshold (always fails); other actions → min_committee_size floor check (bypassed in bootstrap phase PV==9) then quorum tally. `accepted_by_dreps()` / `accepted_by_spo()` predicates; `ratify_action()` combines all three. `LedgerState::enact_action()` method wraps `enact_gov_action()` to avoid split-borrow issues when enacting within epoch boundary processing. `GovernanceActionState::record_vote()` records a voter's vote for testing and block application. Reference: `Cardano.Ledger.Conway.Rules.Ratify`. Comprehensive unit-test coverage (343 unit tests) now includes: proposal validation edge cases (empty ParameterChange, zero drep_deposit/min_committee_size/gov_action_lifetime/drep_activity/committee_term_limit, malformed PoolVotingThresholds/DRepVotingThresholds bundles, valid ParameterChange acceptance, deposit mismatch, unregistered return account, zero treasury withdrawals, treasury withdrawal to unregistered account, treasury withdrawal network mismatch, conflicting committee update, committee member expiration at current epoch, forward self-reference and forward reference in same tx); vote mechanics (vote recast overwrites, vote casting touches DRep activity, DRep unregistration removes stored votes across multiple actions, collect_conway_unregistered_drep_voters extraction and deduplication); voter existence checks (DRepScript, CommitteeScript, StakePool registered/unregistered); complete post-bootstrap voter permission matrix (SPO on Treasury/NoConfidence/HardFork/UpdateCommittee/NewConstitution, committee on most actions + rejected on NoConfidence, DRep accepted on all 7 action types); conway_unit_interval_well_formed validation; conway_pv_can_follow version sequencing (major/minor increment, downgrade/same-version/major-jump rejection); plus all prior DRep inactivity boundary, tally edge case, ratification acceptance/rejection, VoteTally threshold, and parameter-group classification tests. Enactment edge-case tests (9 new) cover: remove nonexistent committee member (returns `members_removed: 0`), no-confidence on empty committee (CommitteeRemoved + quorum reset), multi-field parameter change (3+ fields applied atomically), treasury withdrawals zero-amount skipped, treasury withdrawals exceeding treasury (saturating_sub to 0), add existing committee member (returns `members_added: 0`), hard-fork lineage chain (v10→v11 with lineage advancing), parameter-change lineage chain (two sequential changes preserve earlier fields), parameter-change initializes defaults when protocol_params is None. Certificate processing unit tests (26 new) cover: AccountRegistrationDeposit, AccountUnregistrationDeposit, DelegationToStakePoolAndDrep (dual delegation), AccountRegistrationDelegationToStakePool, AccountRegistrationDelegationToDrep (AlwaysAbstain builtin), AccountRegistrationDelegationToStakePoolAndDrep (AlwaysNoConfidence builtin), DrepRegistration+Unregistration lifecycle, DrepUpdate with anchor, PoolRegistration (deposit accounting), PoolRegistration error paths (cost too low, invalid margin, reward network mismatch, metadata URL >64 bytes), PoolRetirement (within eMax), PoolRetirement error paths (epoch too far, not registered), CommitteeAuthorization (hot key), CommitteeAuthorization for unknown member, CommitteeResignation, CommitteeResignation already resigned, StakeCredential already registered, StakeCredential unregister not registered, delegate to unregistered pool (Shelley accepts per upstream, Conway rejects via `DelegateeStakePoolNotRegisteredDELEG`), DRep already registered, delegate to unregistered DRep, withdrawal balance credited correctly, Conway VRF key uniqueness (duplicate VRF rejected, re-registration same key accepted, Shelley duplicate accepted).
- `protocol_params.rs` voting thresholds: `PoolVotingThresholds` (5 `UnitInterval` fields: motion_no_confidence, committee_normal, committee_no_confidence, hard_fork_initiation, pp_security_group) and `DRepVotingThresholds` (10 `UnitInterval` fields: motion_no_confidence, committee_normal, committee_no_confidence, update_to_constitution, hard_fork_initiation, pp_network_group, pp_economic_group, pp_technical_group, pp_gov_group, treasury_withdrawal) with CBOR array encode/decode. `ProtocolParameters` new fields: `pool_voting_thresholds` (key 25), `drep_voting_thresholds` (key 26), `min_committee_size` (key 27), `committee_term_limit` (key 28).
- `state.rs` `LedgerStateSnapshot` query helpers for LocalStateQuery server dispatch: `query_utxos_by_txin()` (lookup specific TxIns, multi-era precedence over Shelley), `query_stake_pool_ids()` (all registered pool hashes), `query_delegations_and_rewards()` (per-credential delegated pool + reward balance), `query_drep_stake_distribution()` (DRep → total delegated stake map).
- `plutus_validation.rs` `MissingRequiredDatums` Phase-1 check: `validate_supplemental_datums()` now verifies that every spending input with a datum_hash in the UTxO has a corresponding datum entry in the witness set BEFORE the supplemental datum check. Matches upstream `missingRequiredDatums` from `Cardano.Ledger.Alonzo.Rules.Utxow`. Wired into all Alonzo/Babbage/Conway block-apply and submitted-tx paths.
- `state.rs` `ExtraneousScriptWitness` Babbage+ reference script deduction: `validate_no_extraneous_script_witnesses()` and `_typed()` now take `reference_script_hashes: Option<&HashSet<[u8; 28]>>`. For Babbage/Conway, `collect_reference_script_hashes()` collects script hashes from reference input UTxOs. The extraneous check now computes `neededNonRefs = sNeeded \ sRefs` before checking `sReceived ⊆ neededNonRefs`, matching upstream `extraneousScriptWitnessesUTXOW` in Babbage. Pre-Babbage eras pass `None`.
- `state.rs` Shelley script witness validation: `apply_shelley_block()` and the Shelley submitted-tx path now perform native (MultiSig) script witness validation — collecting required script hashes from inputs/certs/withdrawals, evaluating native scripts, checking required-script coverage, and rejecting extraneous scripts. Matches upstream Shelley UTXOW rule (`validateFailedNativeScripts` / `validateMissingScripts` / `extraneousScriptWitnessesUTXOW`). Previously only Allegra+ paths had these checks.
- `state.rs` Conway committee membership unconditional check: `authorize_committee_hot_credential()` and `resign_committee_cold_credential()` now ALWAYS verify `is_current_member || is_potential_future_member` before accepting the operation, matching upstream `checkAndOverwriteCommitteeMemberState` from `Cardano.Ledger.Conway.Rules.GovCert`. Auto-registered stale entries (`expires_at: None`, not in current committee or pending proposals) are correctly rejected. Check ordering matches upstream: resignation checked FIRST, then enacted membership.
- `state.rs` Committee resignation preservation across enactment: `register_with_term()` preserves existing authorization/resignation state when updating an existing entry's term (upstream `committeeMembers` vs `csCommitteeCreds` separation). `NoConfidence` enactment uses `clear_all_membership()` (resets `expires_at` without removing entries). `UpdateCommittee` `members_to_remove` uses `clear_membership()`. `is_enacted_member()` method (`expires_at.is_some()`) filters `tally_committee_votes` and `count_active_committee_members`. Reference: upstream `checkAndOverwriteCommitteeMemberState` + `updatedCommittee` from `Cardano.Ledger.Conway.Rules.Gov` / `Enact`.
- `state.rs` Conway `RefundIncorrectDELEG` PV split: `AccountUnregistrationDeposit` now returns `RefundIncorrectDELEG` for post-bootstrap phase (PV >= 10, `!ctx.bootstrap_phase`) and `IncorrectKeyDepositRefund` for bootstrap phase (PV == 9), matching upstream `hardforkConwayDELEGIncorrectDepositsAndRefunds` from `Cardano.Ledger.Conway.Rules.Deleg`.
- `state.rs` withdrawal/cert ordering parity: `apply_certificates_and_withdrawals_with_future()` now drains reward-account withdrawals BEFORE processing certificates, matching the upstream Conway CERTS recursive STS base case (`conwayCertsTransition` in `Cardano.Ledger.Conway.Rules.Certs`) where `drainAccounts withdrawals` executes in the `Empty` branch before any `CERT` sub-transition. This ordering is semantically relevant: a transaction that withdraws from a reward account AND unregisters the same credential succeeds because draining sets the balance to zero before the `StakeCredentialHasRewards` check. At PV >= 11 (`hardforkConwayMoveWithdrawalsAndDRepChecksToLedgerRule`) the drainage is lifted to the LEDGER rule but still precedes certificate processing. Stake registration creates a zero-balance `RewardAccount` keyed by the configured network id, matching upstream `Accounts` entries keyed by staking credential. After withdrawal-network validation, account lookup/draining resolves the registered account by staking credential rather than exact `RewardAccount` key, mirroring `withdrawalsThatDoNotDrainAccounts` / `drainAccounts` in `Cardano.Ledger.State.Account`.
- `cbor.rs` raw-byte span helper: `extract_block_tx_byte_spans(raw_block: &[u8]) -> Result<BlockTxRawSpans, LedgerError>` walks the outer block CBOR (`[header, [* tx_body], [* witness_set], …]`) and returns `BlockTxRawSpans { bodies, witness_sets }` containing the on-wire byte spans of every transaction body and witness set, byte-for-byte identical to what the block author serialised. **Required** for fee validation and tx-id computation because re-serialising from typed `ShelleyTxBody`/`ShelleyWitnessSet` produces byte-canonical CBOR that does NOT always agree with the on-wire encoding (definite vs indefinite length, set vs array, integer-width canonicalisation), and `min_fee = a · txSize + b` is sensitive to that drift. Surfaced 2026-04-27 by a 30-min preprod sync rehearsal that crashed at slot ~518 460 (Byron→Shelley boundary) with a 440-lovelace fee gap; details in [`docs/REAL_PREPROD_POOL_VERIFICATION.md`](../../../docs/REAL_PREPROD_POOL_VERIFICATION.md). Reference: `Cardano.Ledger.Shelley.Tx.minfee`, `Cardano.Ledger.Core.txIdTxBody`. 4 regression tests in `cbor::tests::extract_block_tx_byte_spans_*` including a deliberately mismatched indefinite-length-array case proving on-wire byte preservation.
- `cbor.rs` decoder allocation discipline: `vec_with_safe_capacity` (soft cap), `vec_with_strict_capacity` (hard cap, returns `LedgerError::DecodedCountTooLarge`), `hashmap_with_safe_capacity`, `check_decoded_count` are the workspace primitives every CBOR decoder uses to gate allocations against attacker-supplied `count: u64` fields. Per-protocol bounds live in `crates/network/src/protocol_size_limits.rs`. Closes audit findings C-1 (handshake `Vec::with_capacity(u64::MAX)` abort) and H-1 (49 sites across handshake, ChainSync, TxSubmission, PeerSharing, NtC peer, all era block decoders, and Byron tx decoder). Reference: `crates/ledger/src/cbor.rs:226-260`.
- `utxo.rs` value-preservation arithmetic: `check_coin_preservation`, `checked_sum_coins`, `checked_add2`, `checked_add3` private helpers — all era `apply_*_tx_withdrawals` paths now propagate overflow as `LedgerError::ValueOverflow { site }` rather than silently saturating. Closes audit M-6. Multi-asset balance arithmetic in `check_multi_asset_preservation` retains saturating semantics (bounded by per-asset `maxValueSize` parameter, not peer-controlled).
