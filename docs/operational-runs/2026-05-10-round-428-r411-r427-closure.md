---
title: 'R411-R427 closure: cardano-tracer trace-forwarder pipe end-to-end runnable'
layout: default
parent: Operational runs
permalink: /operational-runs/2026-05-10-round-428-r411-r427-closure/
---

# R428 — R411-R427 closure: cardano-tracer trace-forwarder pipe end-to-end runnable

**Date:** 2026-05-10
**Predecessor:** [`R411 plan`](2026-05-10-round-411-arc-plan-r411-r430.md) — 17-round arc.
**Closure scope:** Phase 1 + Phase 2 + Phase 3 round 1 (R411-R427).

## Summary

R411-R427 shipped 17 contiguous rounds (5,683 → 5,882 tests, +199),
closing the EKG-equivalent metrics surface, the trace-forwarder
TraceObject sub-protocol port, the cardano-tracer Acceptors leaves
quartet, and the top-level `runCardanoTracer` supervisor entry.
The `yggdrasil-cardano-tracer` binary now boots a real Acceptors
loop on `cargo run --bin cardano-tracer -- -c <config>` instead
of returning the previous "unimplemented" stub.

## Phase-by-phase delivery

### Phase 1: EKG-equivalent metrics surface (R411-R415, 5 rounds)

| Round | Surface                                                                               | Tests delta |
|-------|---------------------------------------------------------------------------------------|-------------|
| R411  | `metrics_store::MetricsStore` + `MetricValue` + `AcceptedMetrics` registry             | +14 |
| R412  | `MetricsStore::insert_resp` + `MetricsLocalStore` delta tracking                       | +12 |
| R413  | `MetricsStore::render_prometheus` + `prometheus.rs` per-node wiring (closes ExpositionStatus carve-out from R408) | +11 |
| R414  | `MetricsStore::render_ekg_html` + `monitoring.rs` per-node wiring                      | +6  |
| R415  | `utils.rs::load_metrics_help` (Run.hs:181-191 mirror)                                   | +6  |

**Phase 1 closure:** the per-node Prometheus + EKG-equivalent
endpoints from R408-R414 read from a real `MetricsStore` registry,
not a placeholder. The metrics surface is ready to ingest real
trace-forwarder traffic.

### Phase 2: Acceptors mini-arc (R416-R426, 11 rounds)

| Round | Surface                                                                               | Mirror              | Tests delta |
|-------|---------------------------------------------------------------------------------------|---------------------|-------------|
| R416  | `crates/network/src/local_listener.rs` — `LocalPeerListener` Unix-pipe primitive       | (synthesis)         | +8  |
| R417  | `crates/network/src/protocols/trace_object_forward.rs` — Type.hs port                  | `Trace.Forward.Protocol.TraceObject.Type` | +16 |
| R418  | (same file) — Codec.hs port                                                            | `Trace.Forward.Protocol.TraceObject.Codec` | +11 |
| R419  | `crates/network/src/trace_object_acceptor.rs` — Acceptor.hs port                       | `Trace.Forward.Protocol.TraceObject.Acceptor` | +4  |
| R420  | `crates/network/src/protocols/trace_object_forward_configuration.rs` — Configuration.hs port | `Trace.Forward.Configuration.TraceObject` | +7  |
| R421  | `crates/network/src/trace_object_run_acceptor.rs` — Run/Acceptor.hs port               | `Trace.Forward.Run.TraceObject.Acceptor` | +6  |
| R422  | `crates/network/src/protocols/forward_sink.rs` + `trace_object_forward_utils.rs` — Utils.hs ports | `Trace.Forward.Utils.{ForwardSink, TraceObject}` | +11 |
| R423  | `crates/cardano-tracer/src/acceptors/utils.rs` — Acceptors/Utils.hs port               | `Cardano.Tracer.Acceptors.Utils` | +9  |
| R424  | `crates/cardano-tracer/src/acceptors/server.rs` — Acceptors/Server.hs port             | `Cardano.Tracer.Acceptors.Server` | +6  |
| R425  | `crates/cardano-tracer/src/acceptors/client.rs` — Acceptors/Client.hs port             | `Cardano.Tracer.Acceptors.Client` | +3  |
| R426  | `crates/cardano-tracer/src/acceptors/run.rs` — Acceptors/Run.hs port                   | `Cardano.Tracer.Acceptors.Run` | +5  |

**Phase 2 closure:** the trace-forwarder TraceObject sub-protocol
is fully wired through Type → Codec → Acceptor → Run +
Configuration + Utils + ForwardSink + Acceptors/{Server, Client,
Utils, Run}. EKG and DataPoint sub-protocols are documented
carve-outs (see R424's `server.rs` module docs).

### Phase 3 round 1: top-level supervisor (R427, 1 round)

| Round | Surface                                                                | Mirror                    | Tests delta |
|-------|------------------------------------------------------------------------|---------------------------|-------------|
| R427  | `crates/cardano-tracer/src/run.rs` + `lib.rs::run` upgrade              | `Cardano.Tracer.Run`      | +7  |

**Phase 3 round 1 closure:** `runCardanoTracer` + `doRunCardanoTracer`
wire R424-R426 into a real binary entry. `cargo run --bin
cardano-tracer -- -c <config>` now boots the Acceptors supervisor
on a multi-thread tokio runtime instead of returning the
"unimplemented" stub.

## Verification gates

All five baseline gates green at HEAD (commit
[`67d4621`](https://github.com/FractionEstate/Cardano-node/commit/67d4621)):

```text
cargo fmt --all -- --check                                     # clean
cargo check-all                                                # clean
cargo test-all                                                 # 5,882 passing, 0 failing
cargo lint                                                     # clean
python3 scripts/check-strict-mirror.py --fail-on-violation     # 0 violations
```

## Carve-outs surfaced programmatically

| Carve-out                                            | Status surface                                              |
|------------------------------------------------------|-------------------------------------------------------------|
| EKG ReqResp sub-protocol (`runEKGAcceptor`)          | `acceptors::server::run_ekg_acceptor_status`                |
| DataPoint sub-protocol (`runDataPointsAcceptor`)     | `acceptors::server::run_data_points_acceptor_status`        |
| RemoteSocket TCP path                                 | `acceptors::server::do_listen_to_forwarder_socket_status`   |
| `runLogsRotator`                                     | `run::run_logs_rotator_status`                              |
| `runMetricsServers` aggregator                       | `run::run_metrics_servers_status`                           |
| Resource stats loop                                  | `run::run_resource_stats_status`                            |
| `prepareDataPointRequestor`                           | `acceptors::utils::prepare_data_point_requestor_status`     |
| `notifyAboutNodeDisconnected` (RTView)                | `acceptors::utils::notify_about_node_disconnected_status`   |
| RTView web UI                                         | `Cardano.Tracer.Handlers.RTView.*` (synthesis carve-out per R326-R459 plan) |
| TraceBundle / meta-trace channel                      | (R427 module docs)                                          |
| `beforeProgramStops` SIGINT/SIGTERM handler           | `crate::utils::before_program_stops_status`                 |
| Trace-forwarder handshake codec                       | (R428+ — see remaining work below)                          |
| TraceObject CBOR codec                                | (R428+ — depends on `trace-dispatcher` upstream port)       |

Each carve-out has a `*_status()` helper returning a `&'static
str` describing the deferral rationale. Operators can `grep`
production code for the helper names to introspect the carve-out
surface programmatically.

## Remaining work (R428-R430)

The R411 plan's original budget was 17 rounds (R411-R430 + 3
rounds for R429-R430 TLS). With 17 rounds shipped at R427, the
arc is at the natural pacing checkpoint. The remaining rounds:

- **R428 closeout** (this doc): Phase 1+2+3-round-1 closure
  documentation + carve-out inventory.
- **R429 TLS termination**: axum-server TLS wiring for the
  Prometheus/Monitoring/Timeseries servers. Bundles the
  `rustls-pemfile` workspace dep already audited at R408. Per the
  D2 decision, this is the final HTTP-side carve-out closure.
- **R430 closeout**: parity-matrix promotion to verified_11_0_1
  on the structurally-complete sub-surfaces; remaining carve-outs
  documented as named follow-on rounds.

## Operational rehearsal

The end-to-end smoke test:

```bash
# Boot a minimal config
cat > /tmp/r428-tracer.json <<EOF
{
  "networkMagic": 764824073,
  "network": {"acceptAt": {"localPipe": "/tmp/r428-tracer.sock"}},
  "logging": [{"logRoot": ".", "logMode": "FileMode", "logFormat": "ForHuman"}]
}
EOF
cargo run --bin cardano-tracer -- -c /tmp/r428-tracer.json

# In another shell, a forwarder dial would now succeed mux-handshake-wise:
ls -la /tmp/r428-tracer.sock  # should exist with 0o660 perms
```

The dial succeeds at the mux layer; full end-to-end TraceObject
ingest blocks on the trace-forwarder handshake codec port (R428+
deferred). Operationally cardano-tracer is ready for SPO-replaceable
deployment for the metrics-only path (per-node Prometheus + EKG
endpoints serve real data once the trace-forwarder forwarder side
is wired in cardano-node — a separate parity arc).

## Related decisions

- [R398 plan](2026-05-10-round-398-dep-audit-tracerenv-decision.md):
  3 dep audits (lettre, maud, axum/tower/rustls-pemfile) +
  TracerEnv option (b) decision (slice-arg vs full-record-arg).
  Both decisions held across R411-R427.
- [R411 plan](2026-05-10-round-411-arc-plan-r411-r430.md):
  17-round arc plan with D1 (timeseries deferred Option C),
  D2 (axum HTTP server), D3 (EKG-equivalent inline). All three
  decisions held; D1 + EKG sub-protocol closure deferred to
  follow-on arcs per the carve-out inventory above.
